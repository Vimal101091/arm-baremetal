{
  "name": "Ubuntu",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "postCreateCommand": "sudo apt update && sudo apt install -y qemu-system-arm gcc-arm-none-eabi make python3-pip git && pip3 install pygments --break-system-packages && wget -P ~ https://git.io/.gdbinit",

  "customizations": {
    "vscode": {
      "extensions": [
        "marus25.cortex-debug",
        "ms-vscode.cpptools",
        "ms-python.python"
      ]
    }
  },

  "remoteUser": "vscode"
}